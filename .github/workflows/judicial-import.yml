name: Judicial Yuan Historical Import

on:
  push:
    branches: [main]
    paths:
      - ".github/workflows/judicial-import.yml"
      - "scripts/import_judicial.py"
      - "scripts/import_judicial_parallel.py"
  workflow_dispatch:
    inputs:
      max_months:
        description: "Maximum months per worker in this run"
        required: false
        default: "4"
  schedule:
    - cron: "*/30 * * * *"

permissions:
  contents: read
  id-token: write

concurrency:
  group: judicial-yuan-history-import-v2
  cancel-in-progress: false

jobs:
  import:
    strategy:
      fail-fast: false
      max-parallel: 4
      matrix:
        worker: [1, 2, 3, 4]
    runs-on: ubuntu-latest
    timeout-minutes: 330
    env:
      WORKER_URL: https://dkqwnkplpgiauqknwbci.supabase.co/functions/v1/judicial-github-worker
      CLAIM_URL: https://dkqwnkplpgiauqknwbci.supabase.co/functions/v1/judicial-github-claim
      MAX_MONTHS: ${{ github.event.inputs.max_months || '4' }}
      MAX_RUNTIME_SECONDS: "18000"
      WORKER_SLOT: ${{ matrix.worker }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install extraction tools
        run: |
          sudo apt-get update
          sudo apt-get install -y unar p7zip-full

      - name: Install Python dependencies
        run: |
          python -m pip install --upgrade pip
          pip install requests

      - name: Import Judicial Yuan monthly archives
        run: python scripts/import_judicial_parallel.py
